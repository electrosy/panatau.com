# panatau.com

Pictures of Romania. Static site on GitHub Pages.

Custom domain: panatau.com

`robots.txt` and `sitemap.xml` sit at the repo root for GitHub Pages. After adding a public HTML page, regenerate the sitemap with `node scripts/build-sitemap.js`.
